{
  "type": "object",
  "properties": {
    "url": {"type": "string"},
    "endpoint": {"type": "string", "enum": ["ports", "mempools", "processors", "storage"]},
    "username": {"type": "string"},
    "password": {"type": "string"},
    "verify_ssl": {"type": "boolean"},
    "query_timeout": {"type": "boolean"}
  },
  "required": ["url", "username", "password", "endpoint"]
}