{
  "type": "object",
  "properties": {
    "source": {"type": "string"},
    "interval": {"type": "string", "enum": ["1m", "5m", "1h"]},
    "pipeline_id": {"type": "string"},
    "oids": {"type": "array", "items": {"type": "string"}},
    "send_data_format": {"type": "string", "enum": ["JSON", "DELIMITED"]}
  },
  "required": ["oids", "interval"]
}
