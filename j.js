function convert_timezone(timestamp, timezone) {
  return new Date(new Date(timestamp * 1000).toLocaleString("en-US", {timeZone: timezone})).getTime() / 1000
}

test_kfk_value_const

y
clicks
y

 
 
timestamp_unix
unix
ver Country
Exchange optional_dim ad_type ADTYPE GEN

key1:val1